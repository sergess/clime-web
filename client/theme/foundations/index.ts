import colors from './colors-foundation.theme';
import radii from './radii-foundation.theme';
import shadows from './shadows-foundation.theme';
import textStyles from './text-styles-foundation.theme';
import sizes from './sizes-foundation.theme';
import breakpoints from './breakpoints-foundation.theme';
import fonts from './fonts-foundation.theme';
import zIndices from './z-indices-foundation.theme';

const foundations = {
  colors,
  radii,
  shadows,
  textStyles,
  sizes,
  breakpoints,
  fonts,
  zIndices,
};

export default foundations;
