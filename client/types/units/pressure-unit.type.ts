export enum PressureUnit {
  INCH = 'inch',
  MM = 'mm',
  MBAR = 'mbar',
}

export const PressureUnitValues = [
  PressureUnit.INCH,
  PressureUnit.MM,
  PressureUnit.MBAR,
];

export default PressureUnit;
